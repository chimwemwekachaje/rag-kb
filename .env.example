# Environment Variables for RAG Knowledge Base

# HuggingFace Token (required for HuggingFace Spaces deployment)
# When set, the app will use HuggingFace sentence-transformers embeddings
# instead of local GGUF models. This is automatically detected in HF Spaces.
HF_TOKEN=your_huggingface_token_here

# Model Paths (optional - defaults to models/ directory)
# These are only used when HF_TOKEN is not set (local deployment)
EMBEDDING_MODEL_PATH=models/nomic-embed-text-v1.5.Q4_K_M.gguf
LLM_MODEL_PATH=models/tinyllama-1.1b-chat-v1.0.Q4_K_M.gguf

# Gradio Configuration (optional)
GRADIO_SERVER_NAME=0.0.0.0
GRADIO_SERVER_PORT=7860

# Python Configuration
PYTHONUNBUFFERED=1
